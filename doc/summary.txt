
Shumi problems:
   Bishops should not be blocked by tewo pawns. Opening only. Make it smaall.
   number of checks is a bad thing? Maybe 4 or greater is bad. Certainly 5 or greater is. 
   Trades bishops too quickly to just double pawns. Msybe make bisjhops worth a little more?
   Not trading what you are ahead in.
   Passed pawns are overprized when they are way passed. Shumi loses them.
   Needs to understand the valueo fadvanced postyed knighst (on 5th rank or more), not expontital.
   semi open files?
   finachetto enuf?
   moving twice same piece in opening
   open files? half open?
   finachetto king side

   not offering 4 checks or more Allow 1 even 2 free, but 3, 4 above exponetially


=============================================================================================================================
=============================================================================================================================

Look. I am having problems with "trading" in Shumi. I have made several attempts at this anf failed each time. Here is a 
failed routine (I am not calling this now)

template<Color c> int MinimaxAI::trade_imbalance_cp_t(int cp_material_all) const
{
    const int cp_material_non_pawn = cp_material_all - cp_score_pawns_only;

    constexpr Color enemy = utility::representation::opposite_color_t<c>;


    const int my_pawns    = engine.game_board.Bits_In[c][Piece::PAWN];
    const int enemy_pawns = engine.game_board.Bits_In[enemy][Piece::PAWN];

    const int my_non_pawn =
        engine.game_board.Bits_In[c][Piece::KNIGHT] +
        engine.game_board.Bits_In[c][Piece::BISHOP] +
        engine.game_board.Bits_In[c][Piece::ROOK]   +
        engine.game_board.Bits_In[c][Piece::QUEEN];

    const int enemy_non_pawn =
        engine.game_board.Bits_In[enemy][Piece::KNIGHT] +
        engine.game_board.Bits_In[enemy][Piece::BISHOP] +
        engine.game_board.Bits_In[enemy][Piece::ROOK]   +
        engine.game_board.Bits_In[enemy][Piece::QUEEN];


    int total_pawns    = my_pawns + enemy_pawns;
    int total_non_pawn = my_non_pawn + enemy_non_pawn;
    if (total_pawns > TRADE_PAWN_CAP) total_pawns = TRADE_PAWN_CAP;
    if (total_non_pawn > TRADE_PIECE_CAP) total_non_pawn = TRADE_PIECE_CAP;
    
    int trade_cp = 0;

    if (cp_score_pawns_only > 0) {
        trade_cp += ((TRADE_PAWN_CAP - total_pawns) * wghts.GetWeight(TRADE_WHEN_AHEAD_PAWNS))
                  / TRADE_PAWN_CAP;
    } else if (cp_score_pawns_only < 0) {
        trade_cp -= ((TRADE_PAWN_CAP - total_pawns) * wghts.GetWeight(TRADE_WHEN_AHEAD_PAWNS))
                  / TRADE_PAWN_CAP;
    }

    if (cp_material_non_pawn > 0) {
        trade_cp += ((TRADE_PIECE_CAP - total_non_pawn) * wghts.GetWeight(TRADE_WHEN_AHEAD_PIECES))
                  / TRADE_PIECE_CAP;
    } else if (cp_material_non_pawn < 0) {
        trade_cp -= ((TRADE_PIECE_CAP - total_non_pawn) * wghts.GetWeight(TRADE_WHEN_AHEAD_PIECES))
                  / TRADE_PIECE_CAP;
    }

    const int cap = wghts.GetWeight(TRADE_IMBALANCE_CAP);

    if (trade_cp >  cap) trade_cp =  cap;
    if (trade_cp < -cap) trade_cp = -cap;

    return trade_cp;

}


But here are my real opinions on this. Suppose I am two pawns up. This means I have two more pawns than the enemy, 
but the same total in pieces. Here I should trade. If 3 pawns ahead a little more motive, but not much. The advance
for being ahead, Should round out , like log(x), and not increase much for larger leads. Howver in that, i am just looking
at the delta in pawns, if the pices are equal. That is vary the delta, and plot y as the eval plus. This should look more like y=log(x)
than y=x. But there are other factors. Keeping the above in mind. This is all, if the piece situation is the same. This is very tricky. Suppose 

I say this: 

if (pieces are the same)
   if (Im ahead in pawns, add a "log(x) type" small bonus)
   else // I dont need to do anything here, as the eval loops over both colors
else
   ??
