OK. Here is the problem. Look at add_knight_moves_to_vector_t(). It uses a table, knight_attack_table, to 
look up the knight moves. I want to do the same thing with pawns, but there are complicatrions. For pawns 
there are 4 tables, one for black and white, 
and one for captures and non captures. now look at add_pawn_moves_to_vector_t(). This is the "hottest" routine of the bunch.
So first I want you TO ONLY add the code to create a pawn capture table. I will add code to read from the tables later. there
should be two tables, one white and one black. Use the same style as much as possible, as the knights table. So the 
only routines changed should be move_tables.hpp move_tables.cpp. 